# Netlify Configuration File for Miss Bella Landing Page
# Ensures correct build settings and flawless SPA routing fallback

[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
